interface:
  display_name: "Redmine"
  short_description: "Use Redmine safely through redmine-cli"
  default_prompt: "Use redmine-cli with an explicit profile; keep reads bounded and use --dry-run plus --yes only for requested non-destructive mutations."
