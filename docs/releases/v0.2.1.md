# redmine-cli v0.2.1

This maintenance release republishes the v0.2 source through the verified
release pipeline. It contains no functional changes beyond v0.2.0.

The release is created from an annotated tag, allowing the pipeline to build,
checksum, and publish its deterministic source bundle and release metadata.
